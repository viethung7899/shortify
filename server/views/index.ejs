<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
  <title>Shortify</title>
</head>
<body>
  <div class="container">
    <h1 class="display-2">Shortify</h1>
    <form action="/" method="POST">
      <div class="mb-3">
        <label class="form-label">URL</label>
        <input type="text" class="form-control" name="url" placeholder="https://www.google.com">
      </div>
      <div class="mb-3">
        <label class="form-label">Name (optional)</label>
        <input type="text" class="form-control" name="name" placeholder="Name">
      </div>
      <button class="btn btn-success" type="submit">Shortify...</button>
    </form>
    <% if (result) { %>
      <% if (result.type === 'SUCCESS') { %>
        <div class="alert alert-success mt-4"><%= result.message %> - 
          <a href=<%= result.url %> target="_blank"><%= result.url %> </a>
        </div>
      <% } else { %>
        <div class="alert alert-danger mt-4"><%= result.message %></div>
      <% } %> 
    <% } %>
  </div>
</body>
</html>